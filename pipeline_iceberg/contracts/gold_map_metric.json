{
  "dataset": "gold_map_metric",
  "purpose": "Frontend heatmap over complete AOI grids, selectable by date, AOI, product, metric and resolution.",
  "grain": "event_date + aoi_id + product_id + metric_id + resolution_km + grid_id",
  "unique_key": [
    "event_date",
    "aoi_id",
    "product_id",
    "metric_id",
    "resolution_km",
    "grid_id"
  ],
  "partition_spec": ["event_date", "aoi_id", "resolution_km"],
  "required_columns": {
    "event_date": "date",
    "aoi_id": "string",
    "product_id": "string",
    "metric_id": "string",
    "resolution_km": "integer",
    "grid_id": "string",
    "grid_row": "smallint",
    "grid_col": "smallint",
    "metric_value": "double",
    "relative_score": "double",
    "display_level": "string",
    "value_source": "string",
    "data_coverage": "double",
    "updated_at_utc": "timestamp",
    "pipeline_version": "string"
  },
  "nullable_columns": [],
  "products": [
    ["CHL", "chlor_a"],
    ["SST", "sea_temperature"],
    ["PRODUCTIVITY", "ocean_productivity_score"],
    ["SUSTAINABILITY", "sustainability_pressure"],
    ["GFW", "fishing_hours"]
  ],
  "resolutions_km": [4, 16, 32],
  "display_contract": {
    "score_range": [0, 100],
    "rank_scope": [
      "event_date",
      "aoi_id",
      "product_id",
      "metric_id",
      "resolution_km"
    ],
    "levels": ["very_low", "low", "medium", "high", "very_high"],
    "fill_order": [
      "observed",
      "grid_<window>d_mean",
      "neighbor_<window>d_mean",
      "aoi_<window>d_mean"
    ],
    "gfw_missing_policy": "zero_filled",
    "note": "Published map scopes never contain null metric_value or relative_score. If any cell remains unavailable, the complete date/AOI/product/metric is omitted at every resolution while other metrics and dates continue."
  },
  "limitations": [
    "All scores are within-day AOI percentiles and do not compare absolute values across dates.",
    "Filled NASA values are display estimates rather than direct observations.",
    "Fishing hours represent apparent fishing effort, not catch.",
    "A missing metric-day partition means the source components were insufficient; it must not be interpreted as a zero value."
  ]
}
